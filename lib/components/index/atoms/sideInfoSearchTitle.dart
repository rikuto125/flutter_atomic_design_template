import 'package:flutter/material.dart';
import '../../commons/atoms/sideInfoTitle.dart';

class SideInfoSearchTitle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: SideInfoTitle(
        title: '検索',
      ),
    );
  }
}
